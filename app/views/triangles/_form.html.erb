<%= form_with(model: triangle) do |form| %>
  <% if triangle.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(triangle.errors.count, "error") %> prohibited this triangle from being saved:</h2>

      <ul>
        <% triangle.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
